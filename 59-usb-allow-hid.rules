# Rules file to allow the logged in user 
# to access usb devices without root priviliges
#
# add to:
#   /etc/udev/rules.d/
#
# For future reference, this is how to allow a device, note TAG+="uaccess" can also be MODE="0666" to allow all users.
#   EXAMPLE: SUBSYSTEM=="usb_device", ATTR{idVendor}=="HEX1", ATTR{idProduct}=="HEX2", TAG+="uaccess" 
#   EXAMPLE: SUBSYSTEM=="usb_device", ATTR{bInterfaceClass}=="03", TAG+="uaccess"
#
# For old kernel versions you need to do SUBSYSTEM=="usb" instead of "usb_device"
#
# This allows all usb devices of class "03", i.e. HID devices.
# Call:
# 1$    sudo udevadm control --reload-rules
# 2$    sudo udevadm trigger
# after adding the rules file.
#
SUBSYSTEM=="usb_device", ATTR{bInterfaceClass}=="03", TAG+="uaccess"
